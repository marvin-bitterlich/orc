{
  "test_config": {
    "mission": {
      "title_template": "Orchestration Test Mission",
      "description": "Automated orchestration test - validates multi-agent coordination",
      "workspace_base": "~/src/missions"
    },
    "grove": {
      "repo": "orc-canary",
      "repo_path": "~/src/orc-canary",
      "worktree_base": "~/src/worktrees"
    },
    "tmux": {
      "session_prefix": "orc-MISSION-TEST",
      "deputy_window": "deputy",
      "imp_window": "imp"
    },
    "test_workload": {
      "parent_wo": {
        "title": "Implement POST /echo endpoint",
        "description": "Add echo endpoint to canary app with tests and documentation"
      },
      "child_wos": [
        {
          "title": "Add POST /echo handler to main.go",
          "description": "Create EchoRequest and EchoResponse structs, implement handleEcho function, register route"
        },
        {
          "title": "Write unit tests for /echo endpoint",
          "description": "Create main_test.go with tests for valid request, invalid JSON, and empty message"
        },
        {
          "title": "Update README with /echo endpoint documentation",
          "description": "Add /echo to endpoint list with request/response examples and curl example"
        },
        {
          "title": "Run tests and verify implementation",
          "description": "Run go test, go build, and manual curl test to verify feature works correctly"
        }
      ]
    },
    "validation": {
      "checkpoints": {
        "phase1_setup": 4,
        "phase2_tmux": 5,
        "phase3_deputy": 4,
        "phase4_work": 3,
        "phase5_implementation": 4,
        "phase6_validation": 5
      },
      "required_files": [
        "main.go",
        "main_test.go",
        "README.md"
      ],
      "test_commands": [
        "go build",
        "go test ./...",
        "curl -X POST http://localhost:8080/echo -d '{\"message\":\"test\"}'"
      ]
    },
    "monitoring": {
      "progress_check_interval_seconds": 30,
      "max_wait_minutes": 30,
      "health_check_commands": [
        "orc status",
        "orc summary"
      ]
    },
    "cleanup": {
      "on_success": true,
      "on_failure": false,
      "preserve_artifacts": [
        "turns/",
        "results.json"
      ]
    }
  }
}
